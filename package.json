{
  "name": "study-groups",
  "version": "1.0.0",
  "description": "App de Grupo de Estudios para Hackatrix",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd study-groups-server && node server.js",
    "heroku-postbuild": "cd study-groups-server && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "path": "^0.12.7"
  }
}
